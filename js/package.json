{
  "name": "tessera-js",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tessera transparency log for Cloudflare Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest --run",
    "test:server": "node --test test/server.spec.js",
    "test:all": "npm test && npm run test:server"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "vitest": "~3.2.0",
    "wrangler": "^4.54.0"
  }
}
